import Foundation

enum ProductIDs {
    static let monthly = "com.sennel.app.premium.monthly"
    static let yearly = "com.sennel.app.premium.yearly"

    static let all: Set<String> = [monthly, yearly]
}
